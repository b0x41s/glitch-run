{
  "name": "tex-glitch-run",
  "private": true,
  "version": "3.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && cp -R bomber-tribe dist/bomber-tribe",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "three": "0.170.0"
  },
  "devDependencies": {
    "vite": "^8.1.5"
  }
}
